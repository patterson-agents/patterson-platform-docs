# Model Context Protocol

The MCP specification is large, versioned, and actively maintained at
https://modelcontextprotocol.io/specification. Committing the full prose here would
go stale faster than it gets read, so this directory holds only the machine artifact:
`schema.json`, the complete JSON schema for protocol revision 2026-07-28. Read the
prose upstream; validate against the schema here.
