# Examples and demos

Working artifacts, not prose. Each subdirectory is one self-contained example that
runs as checked out: a plugin demonstrating one pattern, a generated explorer, an
A2UI stream with the command that produced it. If an example needs a build step to
understand, it belongs in `templates/` instead.
